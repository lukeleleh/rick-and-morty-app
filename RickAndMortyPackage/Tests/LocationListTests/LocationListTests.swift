@testable import LocationList
import XCTest

final class LocationListTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LocationList().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample)
    ]
}
